from Functions import *

conn = Connection()
db = conn.PlayerStats

players = db.Players.find({'player_type':'pitcher'})

i = 1

for player in players:
	print 'processing player ' + str(i) + ' ...'
	dbSplitsUpdate(player)
	i += 1
