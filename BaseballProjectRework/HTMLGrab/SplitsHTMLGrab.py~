'''

This file gets each players splits page's stats
from ESPN

'''

# seems to work ok, now need make it insert into existing document from
# which the url will be obtained and then to separate into functions and
# loop over every player in the database

from Functions import *

conn = Connection()
db = conn.PlayerStats

players = db.Players.find()

i = 1

for player in players:
	if i > 1760:
		print 'processing player ' + str(i) + ' ...'
		dbUpdate(player)
	i += 1

# error after i = 526

# try 807, 526, 1421 later
